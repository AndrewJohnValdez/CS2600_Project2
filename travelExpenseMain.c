#include <stdlib.h>

double milageCalc(doulbe miles);
double parkingFeesCalc(double  parking);
double lodgingCalc(double hotel);
double totalDaysCount(int days);

int main(void) {
    int numberOfDays;
    double departure, arrival;

    return EXIT_SUCCESS;
}
